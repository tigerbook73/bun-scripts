{
  "name": "@tigerbook/run-scripts",
  "version": "0.2.0",
  "type": "module",
  "description": "Fuzzy package script selector for npm/pnpm/yarn/bun workspaces",
  "license": "MIT",
  "author": "tigerbook",
  "keywords": [
    "scripts",
    "fzf",
    "cli",
    "bun",
    "workspace"
  ],
  "bin": {
    "r": "src/index.ts"
  },
  "publishConfig": {
    "bin": {
      "r": "dist/index.js"
    },
    "engines": {
      "node": ">=22.0.0"
    }
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/index.js --target node && sed -i '1s|.*|#!/usr/bin/env node|' dist/index.js && chmod +x dist/index.js",
    "prepack": "bun run build",
    "test": "bun test",
    "lint": "eslint src --fix",
    "lint:check": "eslint src",
    "typecheck": "tsc --noEmit"
  }
}
